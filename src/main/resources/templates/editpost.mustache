<!DOCTYPE html>
	<html>
	{{> head }}
	<body> {{> nav }}
  		<h2>Edit Your Post</h2>
			<div align="center" class="panel-heading">
				<h3 class="panel-title"> {{post.getTitle}}</h3>
			</div>

  		<form method = "post" action="/posts/{{post.getID}}/view">
			Departing Location:<br>
  		<input type="text" name="departure" value= "{{post.getDepartingLocation.getName}}">
			<br><br>
			Arriving Locaiton:<br>
			<input type="text" name="arriving" value= "{{post.getArrivingLocation.getName}}">
			<br><br>
			Date and Time:<br>
			<input type="text" name="date" value= "{{post.getDate}}">
			<br><br>
			Seats Taken:<br>
			<input type="text" name="seats taken" value= "{{post.getSeatsTaken}}">
			<br><br>
			Total Seats:<br>
			<input type="text" name="total seats" value= "{{post.getRideSeats}}">
			<br><br>
			Ride costs:<br>
			<input type="text" name="cost" value= "{{post.Price}}">
			<br><br>
			<button type="submit">Submit Changes</button>
			<br>
		</form>

		<p> If you click the "Submit Changes" button, your post will be updated with the new information.

  	{{> bootstrap-from-cdn-body }}
	</body>
</html>
